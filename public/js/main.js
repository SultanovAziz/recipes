$(document).ready(function (){});$('#ingredient-select').on('change',function (){     unit = $('#ingredient-select option:selected').data("unit");    $('#ingredient-unit-name').text(unit);});$('#add-step').on('click',function (e){   e.preventDefault();});$('#add-ingredient').on('click',function (e){    // e.preventDefault();    // var mainDiv = document.getElementsByClassName('ingredients-list');    // var secondDiv = document.getElementsByClassName('ingredients-child');    //    // sp = document.createElement("div");    // sp.innerHTML = secondDiv[0].innerHTML;    // for(var i in mainDiv)    // {    //     mainDiv[i].appendChild(sp);    // }});$(document).on('click','.delete-recipe' ,function (e){   e.preventDefault();   id = $(this).data("id");   let categoryId = document.getElementById('filter-active').dataset.id;    $.ajax({        url: 'recipe/delete',        data: {id: id,categoryId : categoryId},        type: 'GET',        success: function(res){            $('#recipe').html(res);        },        error: function(e){            alert('Ошибка! Попробуйте позже');            // alert(e.message);        }    });});$('#menu-flters li').on('click',function (e){    e.preventDefault();    let menuFilters =  document.querySelectorAll('#menu-flters > li');    menuFilters.forEach(function (el){        el.classList.remove('filter-active');        el.id = 'filter-non';    });    this.classList.add('filter-active');    this.id = 'filter-active';    id = $(this).data("id");    $.ajax({        url: 'category/reload',        data: {id: id},        type: 'GET',        success: function(res){            $('#recipe').html(res);        },        error: function(e){            alert('Ошибка! Попробуйте позже');            // alert(e.message);        }    });});