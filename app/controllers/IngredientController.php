<?phpnamespace app\controllers;use template\Recipe\RecipeTemplate;class IngredientController extends AppController{    public function reloadUnitAction(){//        $id = empty($_GET['id']) ? null : $_GET['id'];//        if ($id)//        {//            $unit = \R::getAll('SELECT * FROM unit INNER JOIN ingredients on ingredients.unit_id = unit.id and ingredients.id = ? ',[$id]);////        }        echo '11';    }}